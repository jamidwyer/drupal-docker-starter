name: Organization Schema
type: module
description: Optional schema for Organizations, Services, and Tags (taxonomy).
core_version_requirement: ^10 || ^11
package: Starter Schema
dependencies:
  - drupal:node
  - drupal:taxonomy
  - drupal:text
  - drupal:link
